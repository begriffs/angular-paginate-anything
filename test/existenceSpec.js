define(['../paginated-resource.js'], function (presource) {
  'use strict';
  describe('paginated-resource', function () {
    it('exists', function () {
      return expect(presource.name).toBe('begriffs.paginated-resource');
    });
  });
});
