var AngularPaginatedResource = require('../module'),
  chai       = require('chai'),
  expect     = chai.expect;

describe('AngularPaginatedResource', function() {
  'use strict';
  it('exists', function() {
    expect(AngularPaginatedResource).to.be.an('object');
  });
});
