if (typeof define !== 'function') {
  /* jshint latedef:false */
  var define = require('amdefine')(module);
}

define(function () {
  'use strict';
  return {
    // a: a = function() { ... },
    // b: b = function() { ... }
  };
});
